{% autoescape false %}
<?php

declare(strict_types=1);

namespace {{ namespace }};

use Wise\Core\ApiUi\Service\AbstractGetListUiApiService;
use Wise\Core\ApiUi\Helper\UiApiShareMethodsHelper;
use {{ interfaceNamespace }};

class {{ className }} extends AbstractGetListUiApiService implements {{ interface }}
{
    /**
     * Czy pobrać ilość wszystkich rekordów
     */
    protected bool $fetchTotal = true;

    public function __construct(
        UiApiShareMethodsHelper $sharedActionService,
        private readonly {{ applicationInterface }} $listService,
    ){
        parent::__construct($sharedActionService, $listService);
    }

}

{% endautoescape %}
